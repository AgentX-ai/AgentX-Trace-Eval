{
  "name": "agentx-selfhosted",
  "private": true,
  "workspaces": [
    "packages/*",
    "engine"
  ],
  "resolutions": {
    "**/tsup/esbuild": "^0.28.1"
  },
  "scripts": {
    "test": "yarn workspace @agentx/judge-core build && yarn workspace @agentx/judge-core test && yarn workspace @agentx/engine test",
    "typecheck": "yarn workspace @agentx/judge-core build && yarn workspace @agentx/judge-core typecheck && yarn workspace @agentx/engine typecheck"
  }
}
